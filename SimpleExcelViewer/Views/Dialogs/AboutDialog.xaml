<UserControl
	x:Class="SimpleExcelViewer.Views.Dialogs.AboutDialog"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:base="RW.Base.WPF"
	xmlns:c="clr-namespace:SimpleExcelViewer.Controls"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:local="clr-namespace:SimpleExcelViewer.Views.Dialogs"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	xmlns:root="clr-namespace:SimpleExcelViewer"
	xmlns:wpf="RW.Common.WPF"
	d:Background="White"
	d:DataContext="{d:DesignInstance Type=local:AboutDialogViewModel}"
	Style="{StaticResource DialogStyle}"
	DataContext="{base:DISource Type=local:AboutDialogViewModel}"
	mc:Ignorable="d">

	<UserControl.Resources>
		<Style x:Key="Row" TargetType="wpf:TitleContent" BasedOn="{StaticResource TitleContentBaseStyle}">
			<Setter Property="Foreground" Value="Gray"/>
			<Setter Property="Placement" Value="Left"/>
			<Setter Property="TitleWidth" Value="140"/>
		</Style>
	</UserControl.Resources>

	<StackPanel>
		<Grid>
			<Grid.ColumnDefinitions>
				<ColumnDefinition Width="auto"/>
				<ColumnDefinition Width="*"/>
			</Grid.ColumnDefinitions>

			<Border>
				<Image
					Height="50"
					Margin="8,0"
					VerticalAlignment="Top"
					Source="/Icon.png"/>
			</Border>

			<Border Grid.Column="1">
				<StackPanel Margin="8,0">
					<TextBlock FontSize="24" Text="{x:Static root:AppConfig.DisplayAppName}"/>
					<TextBlock FontSize="14" Text="{Binding AppManager.AppFullVersion}"/>
				</StackPanel>
			</Border>

		</Grid>

		<Separator Margin="0,8"/>

		<Border Margin="2,0">
			<StackPanel>
				<TextBlock Text="By RainbowWolfer (R.Wolfer)"/>
				<c:HyperLink DisplayText="Github" Link="{x:Static root:AppConfig.GithubURL}"/>
			</StackPanel>
		</Border>

		<Separator Margin="0,6,0,2"/>

		<wpf:SpacingStackPanel Margin="4,0" Orientation="Vertical" VerticalSpacing="2">

			<wpf:TitleContent Title="Administrator:" Style="{StaticResource Row}">
				<TextBlock Text="{Binding AdministratorStatus}"/>
			</wpf:TitleContent>

			<wpf:TitleContent Title="Process Architecture:" Style="{StaticResource Row}">
				<TextBlock Text="{Binding ProcessArchitecture}"/>
			</wpf:TitleContent>

			<wpf:TitleContent Title="OS Architecture:" Style="{StaticResource Row}">
				<TextBlock Text="{Binding OSArchitecture}"/>
			</wpf:TitleContent>

			<wpf:TitleContent Title="Build Mode:" Style="{StaticResource Row}">
				<TextBlock Text="{Binding AppManager.BuildMode}"/>
			</wpf:TitleContent>

			<wpf:TitleContent Title="Deployment Mode:" Style="{StaticResource Row}">
				<TextBlock Text="{Binding DeploymentMode}"/>
			</wpf:TitleContent>

			<wpf:TitleContent Title="Runtime:" Style="{StaticResource Row}">
				<TextBlock Text="{Binding FrameworkVersion}"/>
			</wpf:TitleContent>

			<wpf:TitleContent Title="OS:" Style="{StaticResource Row}">
				<TextBlock Text="{Binding OSDescription}"/>
			</wpf:TitleContent>

			<wpf:TitleContent Title="Startup Time:" Style="{StaticResource Row}">
				<TextBlock Text="{Binding AppManager.AppStartupTimeSpan, StringFormat='mm\\:ss\\:fff'}"/>
			</wpf:TitleContent>

			<wpf:TitleContent Title="XML Language:" Style="{StaticResource Row}">
				<TextBlock Text="{Binding AppManager.Language}"/>
			</wpf:TitleContent>

		</wpf:SpacingStackPanel>
	</StackPanel>

</UserControl>
