<UserControl
	x:Class="SimpleExcelViewer.Views.Dialogs.ConfigHighlightDialog"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:base="RW.Base.WPF"
	xmlns:controls="clr-namespace:SimpleExcelViewer.Controls"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:dd="urn:gong-wpf-dragdrop"
	xmlns:local="clr-namespace:SimpleExcelViewer.Views.Dialogs"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	xmlns:wpf="RW.Common.WPF"
	d:Background="White"
	d:DataContext="{d:DesignInstance Type=local:ConfigHighlightDialogViewModel}"
	Style="{StaticResource DialogStyle}"
	DataContext="{base:DISource Type=local:ConfigHighlightDialogViewModel}"
	mc:Ignorable="d">

	<UserControl.Resources>

		<wpf:BindingProxy x:Key="ViewModel" Data="{Binding}"/>

		<wpf:CommandProxy x:Key="RemoveCommand" Command="{Binding RemoveCommand}"/>

		<Style x:Key="ColorButtonStyle" TargetType="Button">
			<Setter Property="BorderThickness" Value="1"/>
			<Setter Property="BorderBrush" Value="Gray"/>
			<Setter Property="Cursor" Value="Hand"/>
			<Setter Property="Template">
				<Setter.Value>
					<ControlTemplate TargetType="Button">
						<Border x:Name="PART_Border"
							BorderThickness="{TemplateBinding BorderThickness}"
							BorderBrush="{TemplateBinding BorderBrush}"
							Background="{TemplateBinding Background}"
							CornerRadius="2">
							<ContentPresenter
								Margin="{TemplateBinding Padding}"
								HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
								VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
								RecognizesAccessKey="True"/>
						</Border>

						<ControlTemplate.Triggers>
							<Trigger Property="IsMouseOver" Value="True">
								<Setter TargetName="PART_Border" Property="BorderThickness" Value="2"/>
							</Trigger>

							<Trigger Property="IsPressed" Value="True">
								<Setter TargetName="PART_Border" Property="Opacity" Value="0.6"/>
							</Trigger>

							<Trigger Property="IsEnabled" Value="False">
								<Setter TargetName="PART_Border" Property="Opacity" Value="0.3"/>
							</Trigger>
						</ControlTemplate.Triggers>
					</ControlTemplate>
				</Setter.Value>
			</Setter>
		</Style>

	</UserControl.Resources>

	<Grid Width="600" Height="400">
		<DockPanel>

			<Border DockPanel.Dock="Top">
				<Button Command="{Binding AddCommand}" Content="Add"/>
			</Border>

			<Border Margin="0,6,0,0">
				<ScrollViewer Padding="0,0,4,0">
					<ItemsControl
						dd:DragDrop.DropTargetScrollViewer="{Binding RelativeSource={RelativeSource AncestorType=ScrollViewer}}"
						dd:DragDrop.IsDragSource="True"
						dd:DragDrop.IsDropTarget="True"
						ItemsSource="{Binding RegexConfigItems}"
						d:ItemsSource="{d:SampleData ItemCount=15}">
						<ItemsControl.ItemsPanel>
							<ItemsPanelTemplate>
								<wpf:SpacingStackPanel Orientation="Vertical" VerticalSpacing="4"/>
							</ItemsPanelTemplate>
						</ItemsControl.ItemsPanel>

						<ItemsControl.ItemTemplate>
							<DataTemplate>

								<Border>
									<Grid>
										<Grid.ColumnDefinitions>
											<ColumnDefinition Width="auto"/>
											<ColumnDefinition Width="auto"/>
											<ColumnDefinition Width="*"/>
											<ColumnDefinition Width="auto"/>
										</Grid.ColumnDefinitions>

										<Border
											Grid.Column="0"
											Width="30"
											Background="LightGray"
											CornerRadius="2"
											ToolTip="Priority">
											<TextBlock VerticalAlignment="Center" TextAlignment="Center">
												<TextBlock.Text>
													<MultiBinding Converter="{StaticResource IListToIndexConverter}">
														<Binding Path="Data.RegexConfigItems" Source="{StaticResource ViewModel}"/>
														<Binding/>
													</MultiBinding>
												</TextBlock.Text>
											</TextBlock>
										</Border>

										<Border Grid.Column="1" Margin="6,0,0,0">
											<wpf:SpacingStackPanel HorizontalSpacing="4" Orientation="Horizontal">
												<Border>
													<CheckBox VerticalAlignment="Center" Content="Back" IsChecked="{Binding EnableBackColor}"/>
												</Border>
												<Button x:Name="BackColorButton"
													d:Background="Red"
													Style="{StaticResource ColorButtonStyle}"
													Width="20"
													Background="{Binding BackColor.Color, Converter={StaticResource ColorToBrushConverter}}"
													IsEnabled="{Binding EnableBackColor}"/>
												<Border>
													<CheckBox VerticalAlignment="Center" Content="Text" IsChecked="{Binding EnableTextColor}"/>
												</Border>
												<Button x:Name="TextColorButton"
													d:Background="Black"
													Style="{StaticResource ColorButtonStyle}"
													Width="20"
													Background="{Binding TextColor.Color, Converter={StaticResource ColorToBrushConverter}}"
													IsEnabled="{Binding EnableTextColor}"/>
											</wpf:SpacingStackPanel>
										</Border>

										<TextBox Grid.Column="2" Margin="6,0" Text="{Binding Regex, UpdateSourceTrigger=PropertyChanged}"/>

										<Border Grid.Column="3">
											<Button Padding="2,0" Command="{StaticResource RemoveCommand}" CommandParameter="{Binding}">
												<wpf:FontIcon Foreground="Red" Text="&#xE894;"/>
											</Button>
										</Border>

										<wpf:ButtonPopup SourceButton="{Binding ElementName=BackColorButton}" VerticalOffset="2">
											<Border Style="{StaticResource PopupBorder}">
												<controls:ColorConfigControl ColorEditItem="{Binding BackColor}"/>
											</Border>
										</wpf:ButtonPopup>

										<wpf:ButtonPopup SourceButton="{Binding ElementName=TextColorButton}" VerticalOffset="2">
											<Border Style="{StaticResource PopupBorder}">
												<controls:ColorConfigControl ColorEditItem="{Binding TextColor}"/>
											</Border>
										</wpf:ButtonPopup>

									</Grid>
								</Border>

							</DataTemplate>
						</ItemsControl.ItemTemplate>

					</ItemsControl>
				</ScrollViewer>
			</Border>

		</DockPanel>
	</Grid>

</UserControl>
