<UserControl
	x:Class="SimpleExcelViewer.Views.Dialogs.LibrariesDialog"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:base="RW.Base.WPF"
	xmlns:controls="clr-namespace:SimpleExcelViewer.Controls"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:local="clr-namespace:SimpleExcelViewer.Views.Dialogs"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	xmlns:wpf="RW.Common.WPF"
	d:Background="White"
	d:DataContext="{d:DesignInstance Type=local:LibrariesDialogViewModel}"
	Style="{StaticResource DialogStyle}"
	DataContext="{base:DISource Type=local:LibrariesDialogViewModel}"
	mc:Ignorable="d">

	<Grid Width="300" Height="200">
		<ScrollViewer HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto">
			<ItemsControl Name="ItemsControl" ItemsSource="{Binding LibraryRecords}" d:ItemsSource="{d:SampleData ItemCount=5}">

				<ItemsControl.ItemsPanel>
					<ItemsPanelTemplate>
						<StackPanel Orientation="Vertical"/>
					</ItemsPanelTemplate>
				</ItemsControl.ItemsPanel>

				<ItemsControl.ItemTemplate>
					<DataTemplate>
						<Grid>
							<Grid.ColumnDefinitions>
								<ColumnDefinition Width="30"/>
								<ColumnDefinition Width="*"/>
								<ColumnDefinition Width="80"/>
								<ColumnDefinition Width="auto"/>
							</Grid.ColumnDefinitions>

							<TextBlock>
								<TextBlock.Text>
									<MultiBinding Converter="{StaticResource IListToIndexConverter}">
										<Binding Path="Items" ElementName="ItemsControl"/>
										<Binding/>
									</MultiBinding>
								</TextBlock.Text>
							</TextBlock>

							<TextBlock Grid.Column="1" Text="{Binding Name}"/>
							<TextBlock Grid.Column="2" Text="{Binding Version}"/>

							<controls:HyperLink Grid.Column="3" DisplayText="Source" Link="{Binding Link}"/>

						</Grid>
					</DataTemplate>
				</ItemsControl.ItemTemplate>

			</ItemsControl>
		</ScrollViewer>


	</Grid>

</UserControl>
