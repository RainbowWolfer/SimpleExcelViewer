<UserControl
	x:Class="SimpleExcelViewer.Views.TabView"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:base="RW.Base.WPF"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:dxmvvm="http://schemas.devexpress.com/winfx/2008/xaml/mvvm"
	xmlns:fgc="clr-namespace:FastWpfGrid;assembly=FastWpfGrid"
	xmlns:local="clr-namespace:SimpleExcelViewer.Views"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	d:DataContext="{d:DesignInstance Type=local:TabViewModel}"
	d:DesignHeight="450"
	d:DesignWidth="800"
	DataContext="{base:DISource Type=local:TabViewModel}"
	mc:Ignorable="d">

	<dxmvvm:Interaction.Behaviors>
		<base:DispatcherServiceEx/>
		<base:TypedUIObjectService Name="UserControlService"/>
	</dxmvvm:Interaction.Behaviors>

	<Grid>
		<Border Visibility="{Binding Parameter.IsLoading, Converter={StaticResource Boolean2VisibilityReConverter}}">
			<fgc:FastGridControl x:Name="MainFastGridControl" IsReadOnly="True" Model="{Binding Parameter.TableModel}"/>
		</Border>

		<Grid Visibility="{Binding Parameter.IsLoading, Converter={StaticResource Boolean2VisibilityConverter}}">
			<Border
				Margin="10,0"
				Padding="8"
				HorizontalAlignment="Center"
				VerticalAlignment="Center"
				Background="#d0d0d0">
				<StackPanel Orientation="Vertical">
					<ProgressBar Height="20" MinWidth="250" IsIndeterminate="True"/>
					<Border Height="8"/>
					<TextBlock d:Text="Loading" Text="{Binding Parameter.StatusReport.Status}" TextAlignment="Center"/>
				</StackPanel>
			</Border>
		</Grid>

	</Grid>

</UserControl>
